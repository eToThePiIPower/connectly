<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <a class="navbar-brand" href="/">Connectly</a>
  <button class="navbar-toggler" type="button"
          data-toggle="collapse" data-target="#navbarMenu"
          aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div id="navbarMenu" class="collapse navbar-collapse">
    <ul class="navbar-nav">
      <li class="nav-item">
        <a class="nav-link" href="/about">About</a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="#">Contact</a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="#">Help</a>
      </li>
    </ul>

    <div class="dropdown-divider"></div>

    <ul class="navbar-nav ml-auto">
      <% if user_signed_in? %>

      <li class="nav-item dropdown">
        <a id="navbarUserMenu" class="nav-link dropdown-toggle" href="#"
           data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
          <%= image_tag current_user.profile.avatar.variant(resize: '32x32') if current_user.profile.avatar.attached? %>
          <%= current_user.username %>
        </a>
        <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarUserMenu">
          <a class="dropdown-item" href="#">Edit Account</a>
          <%= link_to 'Logout', destroy_user_session_path, method: :delete, class: 'dropdown-item' %>
        </div>
      </li>

      <% else %>

      <li class="nav-item">
        <%= link_to 'Sign in', new_user_session_path, class: 'btn btn-outline-light mr-lg-2 my-2 my-lg-0' %>
      </li>
      <li class="nav-item">
        <%= link_to 'Sign up', new_user_registration_path, class: 'btn btn-primary ml-lg-2 my-2 my-lg-0' %>
      </li>

      <% end %>
    </ul>
  </div>
</nav>
